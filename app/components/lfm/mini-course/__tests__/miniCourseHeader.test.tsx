import { renderUi } from "@TestUtils/renderUtils"
import MiniCourseHeader from "../miniCourseHeader"

describe('LFM: MiniCourse Header', () => {
  const setup = (props: any) => renderUi(<MiniCourseHeader {...props} />)
  it('Should have date', () => {

  })
})