interface Props { }

function ColorSwatches(props: Props) {
  const { } = props

  return (
    <div className='w-[545px] bg-primary-400 text-white h-[288px] mr-8'>
      Swatches
    </div>
  )
}

export default ColorSwatches
