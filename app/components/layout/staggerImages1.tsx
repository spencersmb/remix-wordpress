import React from 'react'

interface Props { }

function StaggerImages1(props: Props) {
  const { } = props

  return (
    <div>
      Stagger Images 1
    </div>
  )
}

export default StaggerImages1
